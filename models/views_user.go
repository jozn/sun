package models

/*func View_GetUserViewForUsers(usersIds []int) []*x.PB_UserView  {
    mp := make(map[int]bool)

    for _,id := range usersIds{
        mp[id] = true
    }

    uids := []int{}

    for _,id := range usersIds{
        mp[id] = true
    }



}*/
