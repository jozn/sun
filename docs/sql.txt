////////////////////////////////////////
For add fields of texts use this format:
 ALTER TABLE `ms5`.`user` ADD COLUMN `About2` VARCHAR(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_persian_ci DEFAULT '' NULL; // AFTER `LastName`;
 //////////////////////////////////////
