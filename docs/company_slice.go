// Generated by: gen
// TypeWriter: slice
// Directive: +gen on Company

package company

// Sort implementation is a modification of http://golang.org/pkg/sort/#Sort
// Copyright 2009 The Go Authors. All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found at http://golang.org/LICENSE.

// CompanySlice is a slice of type Company. Use it where you would use []Company.
type CompanySlice []Company

// Where returns a new CompanySlice whose elements return true for func. See: http://clipperhouse.github.io/gen/#Where
func (rcv CompanySlice) Where(fn func(Company) bool) (result CompanySlice) {
	for _, v := range rcv {
		if fn(v) {
			result = append(result, v)
		}
	}
	return result
}

// GroupByString groups elements into a map keyed by string. See: http://clipperhouse.github.io/gen/#GroupBy
func (rcv CompanySlice) GroupByString(fn func(Company) string) map[string]CompanySlice {
	result := make(map[string]CompanySlice)
	for _, v := range rcv {
		key := fn(v)
		result[key] = append(result[key], v)
	}
	return result
}

// DistinctBy returns a new CompanySlice whose elements are unique, where equality is defined by a passed func. See: http://clipperhouse.github.io/gen/#DistinctBy
func (rcv CompanySlice) DistinctBy(equal func(Company, Company) bool) (result CompanySlice) {
Outer:
	for _, v := range rcv {
		for _, r := range result {
			if equal(v, r) {
				continue Outer
			}
		}
		result = append(result, v)
	}
	return result
}

// SortBy returns a new ordered CompanySlice, determined by a func defining ‘less’. See: http://clipperhouse.github.io/gen/#SortBy
func (rcv CompanySlice) SortBy(less func(Company, Company) bool) CompanySlice {
	result := make(CompanySlice, len(rcv))
	copy(result, rcv)
	// Switch to heapsort if depth of 2*ceil(lg(n+1)) is reached.
	n := len(result)
	maxDepth := 0
	for i := n; i > 0; i >>= 1 {
		maxDepth++
	}
	maxDepth *= 2
	quickSortCompanySlice(result, less, 0, n, maxDepth)
	return result
}

// SelectString projects a slice of string from CompanySlice, typically called a map in other frameworks. See: http://clipperhouse.github.io/gen/#Select
func (rcv CompanySlice) SelectString(fn func(Company) string) (result []string) {
	for _, v := range rcv {
		result = append(result, fn(v))
	}
	return
}

// Sort implementation based on http://golang.org/pkg/sort/#Sort, see top of this file

func swapCompanySlice(rcv CompanySlice, a, b int) {
	rcv[a], rcv[b] = rcv[b], rcv[a]
}

// Insertion sort
func insertionSortCompanySlice(rcv CompanySlice, less func(Company, Company) bool, a, b int) {
	for i := a + 1; i < b; i++ {
		for j := i; j > a && less(rcv[j], rcv[j-1]); j-- {
			swapCompanySlice(rcv, j, j-1)
		}
	}
}

// siftDown implements the heap property on rcv[lo, hi).
// first is an offset into the array where the root of the heap lies.
func siftDownCompanySlice(rcv CompanySlice, less func(Company, Company) bool, lo, hi, first int) {
	root := lo
	for {
		child := 2*root + 1
		if child >= hi {
			break
		}
		if child+1 < hi && less(rcv[first+child], rcv[first+child+1]) {
			child++
		}
		if !less(rcv[first+root], rcv[first+child]) {
			return
		}
		swapCompanySlice(rcv, first+root, first+child)
		root = child
	}
}

func heapSortCompanySlice(rcv CompanySlice, less func(Company, Company) bool, a, b int) {
	first := a
	lo := 0
	hi := b - a

	// Build heap with greatest element at top.
	for i := (hi - 1) / 2; i >= 0; i-- {
		siftDownCompanySlice(rcv, less, i, hi, first)
	}

	// Pop elements, largest first, into end of rcv.
	for i := hi - 1; i >= 0; i-- {
		swapCompanySlice(rcv, first, first+i)
		siftDownCompanySlice(rcv, less, lo, i, first)
	}
}

// Quicksort, following Bentley and McIlroy,
// Engineering a Sort Function, SP&E November 1993.

// medianOfThree moves the median of the three values rcv[a], rcv[b], rcv[c] into rcv[a].
func medianOfThreeCompanySlice(rcv CompanySlice, less func(Company, Company) bool, a, b, c int) {
	m0 := b
	m1 := a
	m2 := c
	// bubble sort on 3 elements
	if less(rcv[m1], rcv[m0]) {
		swapCompanySlice(rcv, m1, m0)
	}
	if less(rcv[m2], rcv[m1]) {
		swapCompanySlice(rcv, m2, m1)
	}
	if less(rcv[m1], rcv[m0]) {
		swapCompanySlice(rcv, m1, m0)
	}
	// now rcv[m0] <= rcv[m1] <= rcv[m2]
}

func swapRangeCompanySlice(rcv CompanySlice, a, b, n int) {
	for i := 0; i < n; i++ {
		swapCompanySlice(rcv, a+i, b+i)
	}
}

func doPivotCompanySlice(rcv CompanySlice, less func(Company, Company) bool, lo, hi int) (midlo, midhi int) {
	m := lo + (hi-lo)/2 // Written like this to avoid integer overflow.
	if hi-lo > 40 {
		// Tukey's Ninther, median of three medians of three.
		s := (hi - lo) / 8
		medianOfThreeCompanySlice(rcv, less, lo, lo+s, lo+2*s)
		medianOfThreeCompanySlice(rcv, less, m, m-s, m+s)
		medianOfThreeCompanySlice(rcv, less, hi-1, hi-1-s, hi-1-2*s)
	}
	medianOfThreeCompanySlice(rcv, less, lo, m, hi-1)

	// Invariants are:
	//	rcv[lo] = pivot (set up by ChoosePivot)
	//	rcv[lo <= i < a] = pivot
	//	rcv[a <= i < b] < pivot
	//	rcv[b <= i < c] is unexamined
	//	rcv[c <= i < d] > pivot
	//	rcv[d <= i < hi] = pivot
	//
	// Once b meets c, can swap the "= pivot" sections
	// into the middle of the slice.
	pivot := lo
	a, b, c, d := lo+1, lo+1, hi, hi
	for {
		for b < c {
			if less(rcv[b], rcv[pivot]) { // rcv[b] < pivot
				b++
			} else if !less(rcv[pivot], rcv[b]) { // rcv[b] = pivot
				swapCompanySlice(rcv, a, b)
				a++
				b++
			} else {
				break
			}
		}
		for b < c {
			if less(rcv[pivot], rcv[c-1]) { // rcv[c-1] > pivot
				c--
			} else if !less(rcv[c-1], rcv[pivot]) { // rcv[c-1] = pivot
				swapCompanySlice(rcv, c-1, d-1)
				c--
				d--
			} else {
				break
			}
		}
		if b >= c {
			break
		}
		// rcv[b] > pivot; rcv[c-1] < pivot
		swapCompanySlice(rcv, b, c-1)
		b++
		c--
	}

	min := func(a, b int) int {
		if a < b {
			return a
		}
		return b
	}

	n := min(b-a, a-lo)
	swapRangeCompanySlice(rcv, lo, b-n, n)

	n = min(hi-d, d-c)
	swapRangeCompanySlice(rcv, c, hi-n, n)

	return lo + b - a, hi - (d - c)
}

func quickSortCompanySlice(rcv CompanySlice, less func(Company, Company) bool, a, b, maxDepth int) {
	for b-a > 7 {
		if maxDepth == 0 {
			heapSortCompanySlice(rcv, less, a, b)
			return
		}
		maxDepth--
		mlo, mhi := doPivotCompanySlice(rcv, less, a, b)
		// Avoiding recursion on the larger subproblem guarantees
		// a stack depth of at most lg(b-a).
		if mlo-a < b-mhi {
			quickSortCompanySlice(rcv, less, a, mlo, maxDepth)
			a = mhi // i.e., quickSortCompanySlice(rcv, mhi, b)
		} else {
			quickSortCompanySlice(rcv, less, mhi, b, maxDepth)
			b = mlo // i.e., quickSortCompanySlice(rcv, a, mlo)
		}
	}
	if b-a > 1 {
		insertionSortCompanySlice(rcv, less, a, b)
	}
}
